# LLM Agent CLI - 开发依赖

# 包含所有生产依赖
-r requirements.txt

# ===== 测试框架 =====
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0

# ===== 代码质量 =====
# 代码格式化
black>=23.11.0
isort>=5.12.0

# 代码检查
flake8>=6.1.0
pylint>=3.0.0
mypy>=1.7.0

# 类型检查
types-pyyaml>=6.0.12
types-requests>=2.31.0

# ===== 打包工具 =====
# PyInstaller（跨平台打包）
pyinstaller>=6.0.0

# ===== 开发工具 =====
# 调试工具
ipdb>=0.13.13
ipython>=8.17.0

# 文档生成
sphinx>=7.2.0
sphinx-rtd-theme>=1.3.0

# ===== 其他工具 =====
# 预提交钩子
pre-commit>=3.5.0

# ===== 浏览器自动化（可选功能） =====
# 注意：browser-use 需要较新的 LangChain 版本，这里会覆盖基础版本
# Browser-use（LLM 浏览器自动化工具）
# 注意：0.11.2 存在 CDP 连接问题，暂时使用 0.10.1
browser-use==0.10.1
# LangChain 生态（browser-use 要求的版本）
langchain>=0.3.25
langchain-core>=0.3.64
langchain-community>=0.0.20  # 保持兼容
# LangChain OpenAI（用于 Browser-use）
langchain-openai>=0.3.21
# LangChain Ollama（browser-use 要求的版本）
langchain-ollama>=0.3.3
# Playwright（浏览器驱动，browser-use 的依赖）
playwright>=1.40.0
# anyio（browser-use 需要）
anyio>=4.9.0
# 注意：安装后需要运行 playwright install chromium 安装浏览器

# ===== Jupyter 工具（可选功能） =====
# Jupyter 客户端（用于在 Jupyter kernel 中执行代码）
jupyter-client>=8.6.0
# IPython kernel（Python kernel 支持）
ipykernel>=6.25.0
# 注意：安装后可能需要运行 python -m ipykernel install --user 注册 kernel








