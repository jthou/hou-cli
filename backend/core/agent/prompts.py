"""
写作相关提示词与约束的单一来源。
时间：2025-03-17；理由：用户要求约束与测试共用同一份定义；方法：集中定义，后端与测试均从此导入。

- WRITING_CONSTRAINTS: 写文约束（自然语言），供 writing_profile 注入、system_prompt 引用
- WRITING_CONSTRAINT_VIOLATION_PATTERNS: 约束违反时的典型输出模式，供测试断言用
"""
from typing import List

# ---------------------------------------------------------------------------
# 写文约束（自然语言）：注入到写作助手的 user 消息，或作为 system prompt 的一部分
# 后端 writing_profile、system_prompt_templates 均使用此列表
# ---------------------------------------------------------------------------
WRITING_CONSTRAINTS: List[str] = [
    "改写时严格沿用户思路展开，不擅自扩展、不堆砌、不发挥",
    "用户给出结构或思路时，只做表述优化与适度补充，不得改变层次或添加用户未要求的章节",
    "不要加用户未要求的总结小节",
]

# ---------------------------------------------------------------------------
# 约束违反模式：当 LLM 违反上述约束时，输出中常出现的典型模式
# 测试脚本用此列表断言输出是否遵守约束，与 WRITING_CONSTRAINTS 语义对应
# ---------------------------------------------------------------------------
WRITING_CONSTRAINT_VIOLATION_PATTERNS: List[str] = [
    "阶段一", "阶段二", "阶段三",
    "例如，某军工", "例如，某金融", "例如，某电商",
    "动态升降级", "安全委员会",
    "### 总结", "## 总结", "总结与建议", "总结与", "总之，合理的",
    "### 边界条件", "## 边界条件",
    "### 实施路径", "## 实施路径", "实施路径：",
    "### 可操作建议", "## 可操作建议", "可操作建议：",  # 用户未要求的擅自扩展
]
