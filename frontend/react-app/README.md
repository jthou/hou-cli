# Hou CLI Web - React + Tailwind

前端已迁移至 React + Tailwind CSS。

## 开发

```bash
# 安装依赖
cd frontend/react-app && npm install

# 开发模式（需先启动后端，代理到 8081）
npm run dev

# 构建
npm run build
```

构建产物输出到 `frontend/web/dist/`，后端会优先服务 React SPA。

